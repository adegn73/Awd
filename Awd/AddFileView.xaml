<UserControl x:Class="Awd.AddFileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Awd"
             mc:Ignorable="d" 
             Background="{x:Static SystemColors.WindowBrush}"
             Width="400">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="23"/> 
        </Style>
    </UserControl.Resources>
    <Grid VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="File Type" />
        <ComboBox ItemsSource="{Binding FileTypes}" SelectedItem="{Binding FileType}" Grid.Column="1" Margin="5,3"/>
        <TextBlock Text="Fixed Offset (hex)" Grid.Row="1"/>
        <TextBox Text="{Binding FixedOffset}" MaxLength="7" Grid.Row="1" Grid.Column="1" Margin="5,3" Height="23" HorizontalContentAlignment="Right"/>
        <TextBlock Text="File" Grid.Row="2"/>
        <TextBox Text="{Binding FilePath}" Grid.Row="2" Grid.Column="1" Margin="5,3,33,3" Height="23" VerticalContentAlignment="Center"/>
        <Button Command="{Binding BrowseFileCommand}" Content="..." Width="25" Grid.Row="2" Grid.Column="1" HorizontalAlignment="Right" Margin="3,3,5,3" Padding="0"/>
        <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right">
            <Button Command="{Binding OkCommand}" Content="Add" IsDefault="True" Width="75"/>
            <Button Command="{Binding CancelCommand}" Content="Cancel" IsCancel="True" Width="75"/>
        </StackPanel>
    </Grid>
</UserControl>
